# FICTIONAL DEMO FIXTURE — this file is NOT a real credential.
#
# It stands in for a local secret in the fetchjack SSRF demonstration so that a
# vulnerable `file://` fetch has something conspicuous to return. It is wholly
# invented, grants access to nothing, and must never be treated as sensitive.
PREVIEW_WORKER_TOKEN=fixture-local-preview-worker-token-not-a-real-secret
